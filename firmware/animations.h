#include "animation.h"

extern unsigned int getAnimationCount();

extern unsigned int getPlaybackMode();

extern uint8_t getDisplayMode();

extern bool loadAnimation(unsigned int index, Animation* animation);
